import { EstablishmentCategory } from './establishment-category.model';

describe('EstablishmentCategory', () => {
  it('should create an instance', () => {
    expect(new EstablishmentCategory()).toBeTruthy();
  });
});
